# Deployment Instructions

1. Upload all files inside this 'hosting' folder to your Unix hosting environment.
2. Run npm install --production (or npm ci --omit=dev) in this folder to install runtime dependencies.
3. Launch the app with npm run start (alias for next start) while staying in this folder.
4. Select Node.js 18+ in the hosting control panel or runtime settings.

